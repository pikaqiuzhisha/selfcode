<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.chargedot.charge.mapper.UserMapper">

    <!--findById-->
    <select id="findById" resultType="com.chargedot.charge.model.User" parameterType="java.lang.Integer">
      SELECT
        u.id AS id,
        u.enterprise_id AS enterpriseId,
        u.type AS type,
        u.balance AS balance,
        u.forbid_status AS forbidStatus,
        u.status AS status,
        u.user_src AS userSrc,
        u.month_card AS monthCard
      FROM dw_user AS u WHERE u.id=#{userId} AND u.is_del=0 AND u.forbid_status=1 LIMIT 1
    </select>
</mapper>